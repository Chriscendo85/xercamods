package xerca.xercapaint.compat;

import net.fabricmc.loader.api.FabricLoader;
import org.objectweb.asm.tree.ClassNode;
import org.spongepowered.asm.mixin.extensibility.IMixinConfigPlugin;
import org.spongepowered.asm.mixin.extensibility.IMixinInfo;

import java.util.List;
import java.util.Set;

/**
 * Only applies the ditto-integration mixins when DeltaMod ({@code delta}) is installed. When it isn't,
 * {@code shouldApplyMixin} returns false, so the mixin classes (which reference delta's {@code DittoItem})
 * are never applied or loaded, and XercaPaint runs standalone with no delta dependency.
 */
public class DittoMixinPlugin implements IMixinConfigPlugin {
    private final boolean deltaLoaded = FabricLoader.getInstance().isModLoaded("delta");

    @Override
    public void onLoad(String mixinPackage) {
    }

    @Override
    public String getRefMapperConfig() {
        return null;
    }

    @Override
    public boolean shouldApplyMixin(String targetClassName, String mixinClassName) {
        return deltaLoaded;
    }

    @Override
    public void acceptTargets(Set<String> myTargets, Set<String> otherTargets) {
    }

    @Override
    public List<String> getMixins() {
        return null;
    }

    @Override
    public void preApply(String targetClassName, ClassNode targetClass, String mixinClassName, IMixinInfo mixinInfo) {
    }

    @Override
    public void postApply(String targetClassName, ClassNode targetClass, String mixinClassName, IMixinInfo mixinInfo) {
    }
}
